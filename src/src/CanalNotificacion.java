/**
 * Canales por los que un cliente puede recibir notificaciones.
 *
 * NOTA DE INTEGRACION: este enum probablemente ya exista en el proyecto.
 * Si es asi, NO dupliques el archivo: usa el que ya esta y descarta esta copia.
 * Se incluye solo para que el modulo de notificaciones quede autocontenido.
 */
public enum CanalNotificacion {
    EMAIL,
    SMS,
    PUSH
}
